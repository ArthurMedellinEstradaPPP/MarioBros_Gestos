# ============================================================
#  Mario Bros Gesture Controller — Dependencias
#  Instalar con: pip install -r requirements.txt
# ============================================================

# Visión por computadora y procesamiento de imagen
opencv-python>=4.8.0

# Detección de manos en tiempo real
mediapipe>=0.10.0

# Simulación de teclado
pyautogui>=0.9.54

# Operaciones matriciales (requerido por OpenCV y MediaPipe)
numpy>=1.21.0

# Captura de pantalla (usado en el agente IA, opcional)
mss>=9.0.1

# Foco automático de ventana en Windows (opcional pero recomendado)
pywin32>=305; sys_platform == "win32"
